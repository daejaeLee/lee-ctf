# wellness-bot-injection

Write this after the flag is verified. Use `ctf-writeup` to produce a concise, reproducible submission-style handoff, but replace the live flag with a redaction, its expected format, or a non-reversible digest. The live value belongs only under `.local/`.
