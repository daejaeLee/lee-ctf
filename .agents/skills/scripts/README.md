# ctf-skills support scripts

This directory restores relative support-file references used by the installed project skills.

`install_ctf_tools.sh` is downloaded verbatim from `daejaeLee/ctf-skills` at the commit recorded in `.ctf/skills.lock.json`. Do not edit it locally; update the pinned source and re-validate the skill snapshot instead.
